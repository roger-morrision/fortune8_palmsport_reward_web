import { __assign } from "tslib";
import { jsx as _jsx } from "react/jsx-runtime";
/**
 * Draw the types and sizes restrictions for the uploading.
 * @param {Object} fileData file data types, minSize, maxSize
 * @returns JSX Element | null
 *
 * @internal
 */
export default function DrawTypes(_a) {
    var types = _a.types, minSize = _a.minSize, maxSize = _a.maxSize;
    if (types) {
        var stringTypes = types.toString();
        var size = '';
        if (maxSize)
            size += "size >= ".concat(maxSize, ", ");
        if (minSize)
            size += "size <= ".concat(minSize, ", ");
        return (_jsx("span", __assign({ title: "".concat(size, "types: ").concat(stringTypes), className: "file-types" }, { children: stringTypes })));
    }
    return null;
}
//# sourceMappingURL=DrawTypes.js.map